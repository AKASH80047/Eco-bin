# TODO

- [x] Analyze Firebase startup crash from runtime logs.
- [x] Inspect Android and Dart Firebase setup files.
- [ ] Add Google Services Gradle integration (`android/build.gradle.kts`, `android/app/build.gradle.kts`).
- [ ] Fix Firebase config file placement/name for Android (`android/app/google-services.json`).
- [ ] Create `lib/firebase_options.dart` and switch initialization to explicit options.
- [ ] Run validation commands (`flutter clean`, `flutter pub get`, `flutter run`).
